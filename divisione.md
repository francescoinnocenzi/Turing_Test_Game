Inno
- Ollama + endpoint get_ll_response, domanda, giudizio
- 2 check
- handle vari + create + database (relativo alle funzioni)  
- autenticazione + cookie + sessioni js (multi e single)
- (eventuale parte di ricezione mesaggi socket)

Luca
- Transformer trova_simile
- punteggi + classifica
- Connection + entrypoint + handle_message + docker

---

## Argomenti generali:

- Ollama + endpoint get_ll_response, domanda, giudizio
- 2 check
- handle vari + create + database (relativo alle funzioni)  
- autenticazione + cookie + sessioni js (multi e single)
- (eventuale parte di ricezione mesaggi socket)